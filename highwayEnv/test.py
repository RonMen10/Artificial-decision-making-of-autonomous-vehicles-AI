from clusterOfKnowledge import *

print(gapExchange(0, 2, example_dict))

ll = ['1', '2', '3']
ld =[]


#print(typeChange(ll))

"Archive"
def index_for_exchange(self):
    agent_id = []
    self.lane_index = self.road.network.get_lane
    for agent in self.ego_vehicles:
        if agent.lane_index in [("nx", "nxs", 0),("nxs", "mid", 0),("mid", "nxr", 0),("ner", "nes", 0),("nes", "ne", 0)]:
            agent_id.append(agent.id)
    return agent_id
"""
def clusterUpdateArchive():
    print("working")
    exchangeDirector(archive)

and agent.id == archive['id'][index]

"roundabout"
theList = [0, 1]

if agent.lane_index in [("nxs", "mid", 0)]:
    if len(archive.get_archive()['id']) > 0:
        print("holashhh...")
        # archive.clusterUpdateArchive()

theList[1] = archive.index_for_exchange(self)"""